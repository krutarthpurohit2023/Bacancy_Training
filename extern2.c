#include "myfile.h"
#include <stdio.h>
#include "myfile2.h"

void printValue(){
    
    for (int i = 0; i < 24; i++)
        printf("%c", x[i]);
    
}

int main(){
printValue();
int c = a+b;
printf("\n%d",c);
return 0;
}
